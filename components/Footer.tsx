import { Footer as FooterComponent } from 'nextra-theme-docs';

/**
 * Renders the site footer with the text "BARQ! Knowledge Base".
 *
 * Wraps the external {@link FooterComponent} to display custom footer content.
 */
export default function Footer() {
  return <FooterComponent>BARQ! Knowledge Base</FooterComponent>;
}
